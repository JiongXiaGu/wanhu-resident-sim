{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wanhu.local/schemas/life-event.schema.json",
  "title": "Wanhu Resident Life Events V2",
  "$defs": {
    "stableId": { "type": "string", "pattern": "^[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)+$" },
    "lifeTagId": { "type": "string", "pattern": "^lifetag\\.[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)*$" },
    "lifeTagList": { "type": "array", "items": { "$ref": "#/$defs/lifeTagId" }, "uniqueItems": true }
  },
  "type": "object",
  "required": ["schema", "items"],
  "properties": {
    "schema": { "const": "wanhu.life-events.v2" },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "weight", "eligibility", "stages"],
        "properties": {
          "id": { "$ref": "#/$defs/stableId" },
          "title": { "type": "string", "minLength": 1 },
          "weight": { "type": "number", "minimum": 0 },
          "recordToHistory": { "type": "boolean" },
          "eligibility": {
            "type": "object",
            "properties": {
              "occupations": { "type": "array", "items": { "$ref": "#/$defs/stableId" }, "uniqueItems": true },
              "occupationGroups": { "type": "array", "items": { "$ref": "#/$defs/stableId" }, "uniqueItems": true },
              "genders": { "type": "array", "items": { "enum": ["male", "female"] }, "uniqueItems": true },
              "minAge": { "type": "integer", "minimum": 0 },
              "maxAge": { "type": "integer", "minimum": 0 },
              "minChildren": { "type": "integer", "minimum": 0 },
              "requireSpouse": { "type": "boolean" },
              "requiredTags": { "$ref": "#/$defs/lifeTagList" },
              "forbiddenTags": { "$ref": "#/$defs/lifeTagList" }
            },
            "additionalProperties": false
          },
          "effects": {
            "type": "object",
            "properties": {
              "addTags": { "$ref": "#/$defs/lifeTagList" },
              "removeTags": { "$ref": "#/$defs/lifeTagList" }
            },
            "additionalProperties": false
          },
          "source": {
            "type": "object",
            "required": ["type", "label"],
            "properties": {
              "type": { "enum": ["city", "family", "work", "weather", "personal"] },
              "label": { "type": "string", "minLength": 1 }
            },
            "additionalProperties": false
          },
          "stages": {
            "type": "array",
            "minItems": 3,
            "maxItems": 3,
            "items": {
              "type": "object",
              "required": ["delayDays", "title", "text"],
              "properties": {
                "delayDays": {
                  "type": "object",
                  "required": ["min", "max"],
                  "properties": {
                    "min": { "type": "integer", "minimum": 0 },
                    "max": { "type": "integer", "minimum": 0 }
                  },
                  "additionalProperties": false
                },
                "title": { "type": "string", "minLength": 1 },
                "text": { "type": "string", "minLength": 1 },
                "activityOverride": { "type": "string" }
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
