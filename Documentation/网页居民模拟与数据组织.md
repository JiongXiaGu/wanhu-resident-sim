# 网页居民模拟与数据组织

## 目标

Web 不是另起一套居民规则，而是用来验证未来 Unity 居民系统的表现与数据契约。

网页需要解决三件事：

1. 快速生成一批可重复的居民
2. 用真实游戏尺寸验证居民 UI 与 LifeLog
3. 让数据格式未来可以继续被 Unity 导出流程复用

网页层可以比 Unity Runtime 更方便调试，但不能反过来要求 Unity 使用低效的数据结构。

---

## 三层结构

整个仓库的数据建议分成三层：

```text
Content / Definitions
        ↓
Generator / Compiler
        ↓
Generated Snapshot
        ↓
Web UI
        ↓
未来 Unity Export
```

### 1. Content / Definitions

人长期维护的源数据。

包括：

- 姓名词库
- 职业定义
- Routine 模板
- 居民生成规则
- Story Markdown

这些文件追求可读、可审查、可版本管理。

### 2. Generated Snapshot

由工具自动生成的验证数据。

Web 不需要每次运行都自己理解全部源文件规则，而是读取标准化快照。

### 3. Web UI

Web 只负责：

- 展示居民
- 推进时间
- 调用模拟器
- 查看 LifeLog
- 检查 UI 密度
- 暴露开发信息

---

## 推荐目录

在现有仓库基础上逐步扩展为：

```text
wanhu-resident-sim/
│
├─ Content/
│  ├─ Stories/                    # 规范 Story Markdown
│  ├─ Names/
│  │  ├─ surnames.json            # 姓氏池
│  │  └─ given-names.json         # 名字/字元素池
│  ├─ Occupations/
│  │  └─ occupations.json         # 职业定义
│  ├─ Routines/
│  │  └─ routine-templates.json   # 普通生活模板
│  └─ Simulation/
│     └─ resident-generation.json # 居民生成规则
│
├─ Schemas/
│  ├─ story.schema.json
│  ├─ resident-snapshot.schema.json
│  ├─ occupation.schema.json
│  └─ routine.schema.json
│
├─ Tools/
│  ├─ StoryCompiler/
│  └─ ResidentGenerator/
│
├─ Web/
│  ├─ src/
│  │  ├─ domain/                  # TS 数据类型与纯逻辑
│  │  ├─ simulation/              # 网页验证用模拟实现
│  │  ├─ data/                    # generated 数据读取
│  │  ├─ resident/                # 居民 UI
│  │  └─ review/                  # DEV / 审查 UI
│  └─ public/generated/
│     ├─ stories.json
│     ├─ definitions.json
│     └─ resident-snapshot.json
│
└─ Documentation/
```

`Web/public/generated/` 里的文件属于工具生成结果，不是人工编辑源。

---

## 源数据设计

### Names

姓名允许重复，因此数据非常简单。

```json
{
  "schema": "wanhu.names.v1",
  "surnames": ["沈", "吴", "陈", "周", "赵"],
  "maleGivenNames": ["安", "砚", "桥", "榆"],
  "femaleGivenNames": ["荷", "贞", "岚", "宁"]
}
```

V1 不做全城唯一性检测。

生成结果只要求同一个 `ResidentSeed` 每次得到同一个姓名。

### Occupations

职业定义保持稳定 ID，显示名称可以修改。

```json
{
  "schema": "wanhu.occupations.v1",
  "items": [
    {
      "id": "occupation.cloth-worker",
      "name": "布庄伙计",
      "workplaceType": "shop.cloth",
      "minLifeStage": "young-adult"
    },
    {
      "id": "occupation.potter",
      "name": "陶工",
      "workplaceType": "workshop.kiln",
      "minLifeStage": "young-adult"
    }
  ]
}
```

源定义只描述职业本身，不把具体某个居民写进职业文件。

### Routine Templates

Routine Templates 不再是当前方向。新的可读展示源改为 Action Presentation：

```text
Content/ResidentActions/resident-action-presentations.json
```

Presentation 只描述一个**已经真实完成的 Behaviour**怎样显示给玩家，第一版字段只需要 Stable ID、cooldown / record policy 和 2～4 个文本 Variant。

它不拥有职业、年龄、财富、家庭、天气等 Eligibility。

Web Demo 不需要实现完整城市 AI，而是用少量确定性的 Action Completed Trace 生成 RecentAction，验证 Action → Record → UI。

### Resident Generation Rules

不要把概率散落在 Web 组件里。

```json
{
  "schema": "wanhu.resident-generation.v1",
  "daysPerYear": 360,
  "lifeStages": [
    { "id": "teen", "minAge": 13, "maxAge": 17 },
    { "id": "young-adult", "minAge": 18, "maxAge": 29 },
    { "id": "adult", "minAge": 30, "maxAge": 44 },
    { "id": "middle-age", "minAge": 45, "maxAge": 59 },
    { "id": "elder", "minAge": 60, "maxAge": 120 }
  ],
  "recentLifeLogCapacity": 12
}
```

以后 Unity 可以使用同样的稳定定义，再编译成更适合 Runtime 的 Blob / 数值结构。

---

## Web 生成快照

网页不直接把源定义当居民实例。

推荐生成：

```text
Web/public/generated/resident-snapshot.json
```

示例：

```json
{
  "schema": "wanhu.resident-snapshot.v1",
  "citySeed": 102938,
  "currentDay": 12480,
  "residents": [
    {
      "id": 1001,
      "seed": 88421,
      "displayName": "沈荷",
      "birthDay": 240,
      "gender": "female",
      "portraitSeed": 55219,

      "occupationId": "occupation.cloth-worker",
      "workplaceId": 301,
      "employmentStartDay": 11200,

      "householdId": 81,
      "fatherId": 0,
      "motherId": 0,
      "spouseId": 1002,
      "childCount": 2,

      "nextUpdateDay": 12483,
      "stateBits": 0,
      "activeStoryId": null,

      "recentLifeLog": [],
      "majorLifeHistory": []
    }
  ],
  "households": [
    {
      "id": 81,
      "homeId": 920,
      "memberIds": [1001, 1002, 1003, 1004]
    }
  ]
}
```

这里有意把 `households` 独立出来。

居民高频状态不需要包含可变长度的家庭成员数组；只有 UI 或低频家庭系统需要时才查询 Household。

---

## Web Snapshot 与 Unity Runtime 不必一模一样

这是重要边界。

Web Snapshot 可以为了调试方便包含：

- displayName
- memberIds
- 完整 LifeLog
- 原始字符串 ID

Unity Runtime 最终可以编译成：

```text
StableHash / ushort / enum / bitmask / BlobAsset / DynamicBuffer
```

例如 Web：

```text
"occupation.cloth-worker"
```

Unity Runtime 可以变成：

```text
OccupationId = 17
```

只要两边来自同一套定义和稳定 ID 即可。

---

## Web 中的居民生成

第一阶段建议不要一上来生成几千居民。

默认生成：

```text
CitySeed + 50 Residents
```

这样方便快速人工检查。

生成流程：

```text
CitySeed
  ↓
ResidentId
  ↓
Hash(CitySeed, ResidentId)
  ↓
ResidentSeed
  ↓
姓名 / BirthDay / PortraitSeed / 职业 / 家庭背景
```

同样的 CitySeed 应得到同样的一批居民，方便截图对比和 Bug 复现。

DEV 面板可以提供：

- 重新生成 CitySeed
- 输入固定 Seed
- 上一个/下一个居民
- 推进 +1 / +10 / +100 天
- 强制执行一次 Life Update
- 强制触发 Story
- 查看原始居民数据

---

## 网页玩家视图

玩家态继续使用当前验证过的左下约 400px 面板。

### Header

只显示最重要身份：

```text
头像  沈荷
      34岁 · 布庄伙计
      河东坊 · 已婚 · 两个孩子
```

### 当前状态

```text
此刻
正在井边打水
```

当前状态来自 `CurrentAction`。测试 Web 可以从 Action Trace 中给出当前行为，但不能再根据职业、休息日或故事文本推导一个“像真的” Activity。

### 最近生活

最近生活展示少量 `RecentAction`：

```text
最近

今日
家里的水不多了，去井边挑了水回来。

3日前
下工后去朋友家坐了一阵。

7日前
去市上添了些粮食。
```

规则：

- 只来自真实 / 测试 Behaviour Complete；
- 一行展示，不把行为树过程写成日志；
- Target / Place 由行为事件提供，不在 Recent 系统中反查；
- 固定小环自然淘汰；
- 普通 RecentAction 不进入人生经历；
- LifeEvent 仍单独显示“正在经历 / 最近发生”。

### 往事

底部只保留入口：

```text
往事 14 >
```

展开后才读取更多历史记录。

---

## 网页开发视图

DEV 信息与玩家 UI 必须分离。

DEV 面板可以显示：

```text
ResidentId
Seed
BirthDay
计算年龄
OccupationId
WorkplaceId
HouseholdId
Father / Mother / Spouse
NextUpdateDay
StateBits
ActiveStory
LifeLog 容量
当前 Story / Branch / Stage
```

并继续保留正文行数、标题行数等 UI 密度检查。

---

## 推荐 Web 代码边界

不要继续把生成、模拟、UI 全写在 `App.tsx`。

建议逐步拆为：

```text
Web/src/
├─ domain/
│  ├─ resident.ts
│  ├─ household.ts
│  ├─ occupation.ts
│  └─ life-log.ts
│
├─ data/
│  └─ generated-data.ts
│
├─ simulation/
│  ├─ resident-generator.ts
│  ├─ resident-life-update.ts
│  ├─ current-activity.ts
│  └─ story-selector.ts
│
├─ resident/
│  ├─ ResidentPanel.tsx
│  ├─ ResidentHeader.tsx
│  ├─ ResidentLifeFeed.tsx
│  └─ ResidentHistory.tsx
│
├─ review/
│  ├─ DevPanel.tsx
│  └─ StoryReviewView.tsx
│
└─ App.tsx
```

其中 `simulation/` 尽量写成纯函数，输入数据，输出结果，不依赖 React。

这样以后这些规则更容易迁移为 Unity 实现或作为算法参考。

---

## 未来扩展顺序

建议按以下顺序增长，而不是一次做完整社会模拟：

### V1

- 姓名
- BirthDay / 年龄
- 职业
- 工作地点
- 家庭/家谱引用
- 稀疏 Life Update
- Routine
- LifeLog
- Story

### V1.1

- 职业变化
- 出生 / 结婚 / 死亡结构命令
- Household 展示
- MajorLifeHistory

### V1.2

- 区域状态影响
- 天气 / 季节影响 Routine
- 更完整的 Story Effect

除非游戏体验验证明确需要，否则不增加全城关系传播与复杂 Needs。
